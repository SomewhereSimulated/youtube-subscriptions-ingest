# Secrets and per-user credentials — never commit these.
.env
.env.local
scripts/.credentials/

# Machine-specific config (raw_dir/wiki_root absolute paths) — regenerated
# per user by `python scripts/youtube_subscriptions.py configure`.
scripts/.youtube_subscriptions_config.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS cruft
.DS_Store
Thumbs.db
